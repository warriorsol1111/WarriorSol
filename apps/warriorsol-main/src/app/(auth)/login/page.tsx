import Login from "@/components/auth/login";

export const metadata = {
  title: "Warrior Sol - Login",
  description: "Login to your account",
};

export default function LoginPage() {
  return <Login />;
}
